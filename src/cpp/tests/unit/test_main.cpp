// Test runner entry point for unit tests
int main() {
    // This will be the main entry for all unit tests
    // For now, just run the order types test
    extern int test_order_types_main();
    return test_order_types_main();
}